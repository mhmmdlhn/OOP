using _03_Constructors;

// Creating an instance of DerivedClass
DerivedClass derivedObject = new DerivedClass(10, 20);

// Accessing methods from the base class
derivedObject.DisplayBaseValue();

// Accessing methods from the derived class
derivedObject.DisplayDerivedValue();